# =============================================================================
# Rollinhead Dashboard — Staging Environment Configuration
# =============================================================================

DATABASE_URL="postgresql://rollinhead:rollinhead_dev_2026@localhost:5432/rollinhead_staging?schema=public"

PORT=4001
NODE_ENV="staging"
CORS_ORIGIN="https://staging.rollinhead.com,http://localhost:3001"

JWT_SECRET="rollinhead-staging-jwt-secret-key-2026"
JWT_EXPIRY="24h"
JWT_REFRESH_SECRET="rollinhead-staging-refresh-secret-key-2026"
JWT_REFRESH_EXPIRY="7d"

UPLOAD_DIR="./uploads_staging"
MAX_FILE_SIZE=10485760
BCRYPT_ROUNDS=10
